# GitHub token with package publishing permissions and private repo access
GH_TOKEN=

# GitHub token for accessing private repositories (can be same as GH_TOKEN)
GITHUB_TOKEN=

# Branch name (main, next, beta, alpha)
BRANCH=main

# Optional: NPM tag (latest, next, beta, alpha)
TAG=latest

# Optional: Environment-specific variables
NODE_ENV=development
